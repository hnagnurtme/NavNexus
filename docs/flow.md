# FLOW.md - Luồng Xử Lý Hệ Thống

## 🎯 Tổng Quan
Hệ thống xử lý file đa ngôn ngữ, xây dựng Knowledge Graph và Vector Database với khả năng caching thông minh.

---

## 📊 FLOW 1: Upload & Processing (Cold Start - File đầu tiên)

```
┌─────────────────────────────────────────────────────────────────┐
│ CLIENT: Upload 5 files đồng thời vào Workspace                  │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ STEP 1: Firestore Check (File Metadata Cache)                  │
│ ─────────────────────────────────────────────────────────────── │
│ Query: workspaces/{workspace_id}/files                          │
│ Check: file_hash (SHA256)                                       │
│                                                                 │
│ ┌─────────────┐                                                │
│ │ File trùng? │                                                │
│ └──────┬──────┘                                                │
│        │                                                        │
│   YES  │  NO                                                    │
│   ┌────▼────┐  ┌──────────────────┐                           │
│   │ RETURN  │  │ Continue to      │                           │
│   │ Cached  │  │ STEP 2           │                           │
│   │ Data    │  └──────────────────┘                           │
│   └─────────┘                                                  │
└─────────────────────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ STEP 2: Upload to NOS Storage                                  │
│ ─────────────────────────────────────────────────────────────── │
│ • Generate unique file_id                                       │
│ • Upload binary to NOS                                          │
│ • Get NOS URL                                                   │
│ • Calculate file_hash (SHA256)                                  │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ STEP 3: Language Detection & Translation                       │
│ ─────────────────────────────────────────────────────────────── │
│ File 1 (Korean)   → Papago API → English                       │
│ File 2 (English)  → ✅ SKIP (already target language)          │
│ File 3 (Japanese) → Papago API → English                       │
│ File 4 (English)  → ✅ SKIP                                     │
│ File 5 (Korean)   → Papago API → English                       │
│                                                                 │
│ Output: translated_text (unified language)                      │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ STEP 4: Qdrant Context Retrieval                               │
│ ─────────────────────────────────────────────────────────────── │
│ Collection: workspace_{workspace_id}                            │
│                                                                 │
│ File 1: ❌ Empty collection → No context                        │
│ File 2: ✅ Has File 1 data → Vector search (top-5)             │
│ File 3: ✅ Has File 1+2 data → Vector search (top-5)           │
│ File 4: ✅ Has File 1+2+3 data → Vector search (top-5)         │
│ File 5: ✅ Has File 1+2+3+4 data → Vector search (top-5)       │
│                                                                 │
│ Query: Embed(translated_text summary)                           │
│ Return: List<QdrantChunk> (similar chunks)                      │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ STEP 5: LLM Processing (HyperCLOVA X)                          │
│ ─────────────────────────────────────────────────────────────── │
│ Input:                                                          │
│   - translated_text                                             │
│   - context (from Qdrant, if available)                         │
│                                                                 │
│ Prompt Pattern:                                                 │
│ ┌─────────────────────────────────────────────────────────────┐│
│ │ # Context from previous documents (if any):                 ││
│ │ {context_chunks}                                             ││
│ │                                                              ││
│ │ # New document to process:                                  ││
│ │ {translated_text}                                            ││
│ │                                                              ││
│ │ # Extract:                                                   ││
│ │ 1. Topics (main subjects)                                    ││
│ │ 2. Concepts (key terms, algorithms, techniques)              ││
│ │ 3. Evidence (quotes, facts, findings)                        ││
│ │ 4. Relationships (how topics connect)                        ││
│ │                                                              ││
│ │ # Maintain consistency with existing knowledge structure    ││
│ └─────────────────────────────────────────────────────────────┘│
│                                                                 │
│ Output:                                                         │
│   - KnowledgeNodes[]                                            │
│   - Evidence[]                                                  │
│   - Chunks[] (chunked text with metadata)                       │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ STEP 6: Dual Storage (Parallel)                                │
│ ─────────────────────────────────────────────────────────────── │
│                                                                 │
│ ┌──────────────────────┐     ┌─────────────────────────────┐  │
│ │ A. QDRANT            │     │ B. NEO4J                    │  │
│ │ ──────────────────── │     │ ─────────────────────────── │  │
│ │ For each chunk:      │     │ For each KnowledgeNode:     │  │
│ │ 1. Embed text        │     │ 1. Create/Merge node        │  │
│ │ 2. Store vector      │     │ 2. Set properties           │  │
│ │ 3. Attach payload:   │     │ 3. Link relationships:      │  │
│ │    - paper_id        │     │    - HAS_CHILD              │  │
│ │    - page            │     │    - HAS_EVIDENCE           │  │
│ │    - quote           │     │ 4. Store qdrant_vector_id   │  │
│ │    - summary         │     │                             │  │
│ │    - concepts[]      │     │ For each Evidence:          │  │
│ │    - topic           │     │ 1. Create node              │  │
│ │                      │     │ 2. Link to KnowledgeNode    │  │
│ │ Return: point_ids[]  │     │                             │  │
│ │                      │     │ Return: node_ids[]          │  │
│ └──────────────────────┘     └─────────────────────────────┘  │
│                                                                 │
└────────┬────────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────────┐
│ STEP 7: Update Firestore Cache                                 │
│ ─────────────────────────────────────────────────────────────── │
│ Path: workspaces/{workspace_id}/files/{file_id}                 │
│                                                                 │
│ Save:                                                           │
│   - file_metadata (name, size, hash, upload_date)              │
│   - nos_url                                                     │
│   - neo4j_node_ids[]                                            │
│   - qdrant_point_ids[]                                          │
│   - processing_status: "completed"                              │
│   - language_detected                                           │
│   - translation_target                                          │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ RETURN to CLIENT                                                │
│ ─────────────────────────────────────────────────────────────── │
│ {                                                               │
│   "file_id": "uuid",                                            │
│   "status": "completed",                                        │
│   "neo4j_nodes": ["topic-ppo", "topic-rl"],                     │
│   "qdrant_points": ["chunk-001", "chunk-002"],                  │
│   "processing_time_ms": 5432                                    │
│ }                                                               │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 FLOW 2: Subsequent File Processing (Warm Start)

```
File N (N > 1) được upload:

STEP 4 (Qdrant Context):
  ✅ Collection đã có data từ File 1, 2, ..., N-1
  
  Query Vector: Embed(file_N summary)
  
  Results:
  ┌─────────────────────────────────────────────────┐
  │ Chunk 1 (from File 1, score: 0.92)             │
  │ - Topic: "PPO Algorithm"                        │
  │ - Summary: "PPO uses clipped objective..."      │
  │                                                 │
  │ Chunk 2 (from File 2, score: 0.89)             │
  │ - Topic: "Actor-Critic"                         │
  │ - Summary: "Actor proposes actions..."          │
  │                                                 │
  │ Chunk 3 (from File 3, score: 0.85)             │
  │ - Topic: "Policy Gradient"                      │
  │ - Summary: "Gradient ascent on rewards..."      │
  └─────────────────────────────────────────────────┘
  
STEP 5 (LLM with Rich Context):
  Prompt:
  """
  # Existing knowledge structure:
  
  ## Topic: PPO Algorithm
  - Concepts: clipped objective, importance sampling
  - Evidence: "PPO constrains policy updates..."
  
  ## Topic: Actor-Critic
  - Concepts: value function, policy network
  - Evidence: "Actor proposes, critic evaluates..."
  
  # NEW DOCUMENT to integrate:
  {file_N content}
  
  # Tasks:
  1. Identify if new concepts belong to existing topics
  2. Create new topics only if truly novel
  3. Extract evidence that supports existing nodes
  4. Maintain naming consistency (e.g., always "PPO", not "Proximal Policy Optimization")
  """
  
  Result: Higher quality, more consistent extraction ✨
```

---

## 🚀 FLOW 3: Query & Inference

```
┌─────────────────────────────────────────────────────────────────┐
│ USER QUERY: "Explain PPO algorithm's advantages"                │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ STEP 1: Semantic Search (Qdrant)                               │
│ ─────────────────────────────────────────────────────────────── │
│ Embed query → Vector search                                     │
│ Return: Top-K relevant chunks                                   │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ STEP 2: Graph Traversal (Neo4j)                                │
│ ─────────────────────────────────────────────────────────────── │
│ Find KnowledgeNode where topic="PPO"                            │
│ Traverse:                                                       │
│   - HAS_CHILD (get sub-topics)                                  │
│   - HAS_EVIDENCE (get supporting quotes)                        │
│   - Related nodes (via shared concepts)                         │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ STEP 3: LLM Synthesis                                           │
│ ─────────────────────────────────────────────────────────────── │
│ Input:                                                          │
│   - User query                                                  │
│   - Qdrant chunks (semantic context)                            │
│   - Neo4j graph (structured relationships)                      │
│   - Evidence nodes (citations)                                  │
│                                                                 │
│ Generate:                                                       │
│   - Synthesized answer                                          │
│   - Citations (Evidence with source URLs)                       │
│   - Related topics to explore                                   │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ RETURN: Structured Response                                     │
│ {                                                               │
│   "answer": "PPO advantages include...",                        │
│   "evidence": [                                                 │
│     {                                                           │
│       "text": "PPO constrains policy updates...",               │
│       "source": "Schulman et al. 2017",                         │
│       "url": "https://..."                                      │
│     }                                                           │
│   ],                                                            │
│   "related_topics": ["Actor-Critic", "TRPO"],                   │
│   "graph_visualization_data": {...}                             │
│ }                                                               │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔍 FLOW 4: Gap Detection (Recommendation Assistant)

```
┌─────────────────────────────────────────────────────────────────┐
│ BACKGROUND JOB: Analyze Knowledge Graph                         │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ Neo4j Cypher Queries:                                           │
│ ─────────────────────────────────────────────────────────────── │
│                                                                 │
│ 1. Find Orphan Nodes:                                           │
│    MATCH (n:KnowledgeNode)                                      │
│    WHERE NOT (n)-[:HAS_CHILD|HAS_EVIDENCE]-()                   │
│    RETURN n                                                     │
│                                                                 │
│ 2. Find Weak Connections:                                       │
│    MATCH (n:KnowledgeNode)-[r:HAS_EVIDENCE]-(e)                 │
│    WITH n, COUNT(e) as evidence_count                           │
│    WHERE evidence_count < 2                                     │
│    RETURN n                                                     │
│                                                                 │
│ 3. Find Broken Bridges:                                         │
│    MATCH (a:KnowledgeNode), (b:KnowledgeNode)                   │
│    WHERE a.concepts INTERSECT b.concepts                        │
│    AND NOT (a)-[:HAS_CHILD]-(b)                                 │
│    RETURN a, b                                                  │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ Generate Recommendations:                                       │
│ ─────────────────────────────────────────────────────────────── │
│ • "Upload papers about: Actor-Critic + PPO connection"          │
│ • "Missing evidence for: Policy Gradient Theorem"               │
│ • "Suggested keywords: trust region, KL divergence"             │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🎨 Key Design Decisions

### ✅ Progressive Context Building
- File 1: Cold start (no context)
- File 2-N: Warm start (context-aware extraction)

### ✅ Language Normalization
- Skip translation if already target language
- Papago API only when needed

### ✅ Dual Storage Sync
- Qdrant: Semantic search
- Neo4j: Graph reasoning
- Linked via `qdrant_vector_id`

### ✅ Firestore Caching
- Avoid reprocessing duplicate files
- Fast metadata lookup
- Store processing status

---

## 📈 Performance Optimization

| Stage | Optimization |
|-------|-------------|
| File Check | Firestore index on `file_hash` |
| Translation | Batch API calls, skip if target lang |
| Vector Search | Qdrant indexing (HNSW) |
| LLM Calls | Context window management, streaming |
| Graph Write | Neo4j batch MERGE operations |

---

## 🛡️ Error Handling

```
Each stage has fallback:
  - NOS Upload fails → Retry 3x → Mark "failed" in Firestore
  - Papago timeout → Use original text → Log warning
  - Qdrant unavailable → Continue without context → Degrade gracefully
  - LLM rate limit → Queue job → Process async
  - Neo4j write fails → Rollback → Retry later
```